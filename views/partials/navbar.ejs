<nav class="navbar navbar-expand-sm navbar-light bg-light">
  <div class="container-fluid">
      <a class="navbar-brand" href="/">With Dogs <i class="fa-solid fa-dog" style="color: #748B6F;"></i></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">
              <a class="nav-link text-green" href="/">Home</a>
              <a class="nav-link text-green" href="/places">Places</a>
              <% if(currentUser) {%>
              <a class="nav-link text-green" href="/places/new">Add Place</a>
              <% }  %>
          </div>
          <div class="navbar-nav ms-auto">
              <% if(!currentUser) {%>
              <a href="/login" class="nav-link text-green">Login</a>
              <a href="/register" class="nav-link text-green">Register</a>
              <% } else { %>
              <a href="/logout" class="nav-link text-green">Logout</a>
              <% } %>
          </div>
      </div>
  </div>
</nav>
